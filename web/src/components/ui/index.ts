/**
 * The platform's Material 3 design system.
 *
 * Colour, shape, elevation and type all come from the `--md-sys-color-*` /
 * `--md-sys-shape-*` / `--md-sys-elevation-*` tokens in `app/globals.css`;
 * nothing here hardcodes a colour. Import from `@/components/ui` — never reach
 * into a module's own folder for a button.
 */

export * from './icons'
export * from './primitives'
